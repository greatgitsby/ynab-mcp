[pytest]
asyncio_mode = auto
minversion = 7.0
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: marks tests as integration tests
addopts =
    -v
    --tb=short
    --strict-markers
