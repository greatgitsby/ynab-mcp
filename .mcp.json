{
  "mcpServers": {
    "ynab": {
      "command": "bash",
      "args": [
        "-c",
        "[ -f .env ] && set -a && source .env && set +a; uv run python -m ynab_mcp.server"
      ]
    }
  }
}
